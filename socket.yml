name: syncano-socket-giphy

endpoints:
  random:
    description: Endpoint for getting giphy content by tag
    inputs:
      properties:
        name:
          type: string
          description: giphy tag

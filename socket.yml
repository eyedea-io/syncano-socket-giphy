name: syncano-socket-giphy

endpoints:
  random:
    description: Endpoint for getting giphy content by tag
    inputs:
      properties:
        name:
          type: string
          description: giphy tag
    outputs:
      success:
        exit_code: 200
        description: Success
      fail:
        exit_code: 400
        description: Failed
